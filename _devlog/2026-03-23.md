# HAL-O Devlog — 2026-03-23

**53 commits** in a single session. Full rebrand, infrastructure buildout, 3D scene overhaul, voice system, demo mode, and CI pipeline.

---

## Migration

- Rebranded from Claudeborn to HAL-O (`e442a2d`)
- Finished migration: fixed script paths, removed migration docs (`c8198da`)
- Organized root: moved concepts, screenshots, dev scripts to subfolders (`ceb828a`)

## Infrastructure

- **Playwright E2E tests**: smoke test, setup-fresh, voice preview (`8b88f16`)
- **Docker testing**: Dockerfile + docker-compose for isolated wizard first-run flow (`a098824`), xauth fix for xvfb-run (`ff30634`)
- **Dev menu**: Docker test launchers (`f3ba364`), switched to `openTerminalAt` (`15cf2e8`), then `execSync` with `shell:'cmd.exe'` for reliable Windows terminal launch (`54f7935`)
- **GitHub Actions CI**: Linux + Windows matrix on master push/PR (`11e8853`), Telegram notifications on pass/fail (`90f1736`, `07a93cc`)

## Terminal

- Bar cursor style, removed `allowTransparency` (`a5a3728`)
- Debounced terminal resize + forced WebGL refresh on tab switch (`a841d0f`)
- Renderer crash guard: auto-reload on crash/unresponsive (`34796d1`)
- Terminal dock position: bottom, right, or left (`c9f5758`)
- Session absorption: detect + absorb external Claude CLI sessions (`5af99ae`)
- Auto-restore limited to 2 most recent sessions on restart (`99d2bda`)
- Extracted `HudTopbar`, `useSettings`, `useTerminalSessions` hooks (`c2dfeff`)

## 3D Scene

### Layouts & Themes
- 4 renderer-specific 3D layouts for PBR/Holo renderers (`7b0751d`)
- 10 holographic layouts: hemisphere, arena, grid wall, DNA helix, cascade, constellation (`a1553cc`)
- 6 renderer themes: tactical, ember, arctic, etc. (`cffe7aa`)
- Full theme refactor: `useThreeTheme` hook + `ThreeThemeContext`, all 3D components consume palette (`109cc83`, `cffe7aa`)

### VFX & Content
- Ambient VFX: DataParticles, HudScrollText, SpaceshipFlyby (`43588c9`)
- VFX spawn frequency slider + slower/bigger spaceship flyby (`8748f8f`)
- Spaceship flyby repeat fix: keep component mounted, toggle visibility (`fe93aa5`)
- Screen content enrichment: git stats, activity bars, file count (`4c0e9c1`)
- Health indicators: edge glow by project status, scrolling background text (`b1698aa`)
- Screen stacks for overflow groups (`b1698aa`)
- Screen opacity slider + group color on edges (`1586f51`)

### Camera & Performance
- Camera tweaking: distance, angle, particle hide wired from settings to PBR scene (`f388fb1`)
- Camera tweak sliders synced with orbit/zoom controls (`a526dab`)
- APPLY/RESET camera buttons + CameraDriver for live reset (`6326a2a`)
- GPU/GC pressure reduced: Vector3 allocs eliminated, reflector 512, dead code removed (`d2dc027`)
- Particle camera exclusion zone: fade out near camera (`b0d9e67`), widened to 4-8 units (`82cdaf6`)
- EffectComposer crash guard: PostFX defers init one frame (`c12d46e`, `4efdecd`)

## Voice

- 20 voice profiles with preview picker in Settings (`7035aa0`)
- Per-voice audio cache for instant preview playback (`907e8d8`)
- Wired voice profile to TTS, added auto-select logic (`1786414`)
- Zog-zog detection triggers orc mode (`1786414`)
- TTS chain reordered: Chatterbox > Voicebox > Edge TTS > ElevenLabs
- Voice preview fixed: base64 data URL instead of `file://` (`4efdecd`, `b357fcc`)

## Wizard & Setup

- Expanded setup screen: detect git, Python, Claude CLI, ffmpeg with one-click install (`44adc4f`, `e41d97a`)
- Import screen: zero-friction, non-destructive project enlistment (`909f8fb`, `4139d6c`)
- Eliminated setup flash with loading state before decision (`7bf9795`)
- 2D Preview Mode via Dev menu (`7bf9795`)
- Adaptive settings panel width (`7bf9795`)
- Relaxed `isFullySetup`: `CLAUDE.md` OR `.claude/` dir is enough (`a1c76b0`)

## Custom Groups

- `useProjectGroups` hook with CRUD + presets (`f92da71`)
- GroupsPanel UI in topbar (`f92da71`)
- Right-click assign to group (`f92da71`)
- Group-aware 3D layouts: grouped-rings, stacked-groups (`f92da71`)
- Group color on screen edges (`1586f51`)

## Demo Mode

- 30 fake projects, 4 scripted terminal feeds (`bd58131`)
- Full settings panel: cards, terminals, tabs, VFX frequency, voice (`bd58131`)
- Background tab pause for demo terminal feeds (`03b3f84`)

## Bug Fixes

- 8 Windows path/cmd.exe escaping issues (`e410041`)
- Git Bash `/d/...` paths normalized to `D:\` for Windows cmd spawn (`eee3b1c`)
- Hub font size now affects topbar, stats, settings labels (`66310ed`)
- EffectComposer crash guard on canvas remount (`c12d46e`)
- `isFullySetup` relaxed (`a1c76b0`)
- Voice playback: base64 data URL in TerminalPanel and VoiceController (`b357fcc`)
- Spaceship flyby repeat (`fe93aa5`)
- PostFX defers init one frame to avoid WebGL race (`4efdecd`)

## CI / Ops

- GitHub Actions workflow: build + lint on Linux and Windows (`11e8853`)
- Telegram notifications on CI pass/fail (`90f1736`)

---

*Session duration: full day. All work on master branch.*
