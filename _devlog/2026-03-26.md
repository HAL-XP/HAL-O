# HAL-O Devlog — 2026-03-26 (Session 6)

**5.5 human hours** (21:00 → 02:30 CET). ~30 background agents spawned. 8 demo video iterations. Major debugging breakthrough + deep process retrospective.

---

## Demo Video Production (V1 → V8)

- **V1-V2**: Camera teleport at animateCamera→OrbitControls handoff (diff 14-16)
- **V3**: Zero-handoff fix — OrbitControls from frame 1, smoothstep interpolation via page.evaluate steps. PASS (max 7.98). But choppy (10fps steps).
- **V4**: Single requestAnimationFrame injection = 60fps smooth. PASS (max 7.6). Ember theme, hal-eye sphere.
- **V5-A/B**: Two variants (tactical+Hal, holographic+Hallie). No audio captured (gdigrab limitation).
- **V6-V7**: VB-Cable installed for audio loopback. Audio track present but silent (-91dB) — Electron routes to default device, not CABLE Input.
- **V8**: **BREAKTHROUGH** — sphere audio reaction CONFIRMED WORKING. Manual AnalyserNode creation fixes the null analyser issue. Frame comparison proves sphere scales with voice. Audio capture still pending (OBS needed).

### Root Causes
1. `setAudioDemo(true)` overrides real FFT with fake sine waves (PbrHoloScene.tsx:953)
2. `window.__haloAudioAnalyser` is NULL in Playwright — lazy init never fires without user interaction
3. gdigrab cannot capture application audio without system loopback device
4. VB-Cable routes to CABLE Output but Electron sends to default device

## Tools Built

- **`_scripts/detect_teleport.py`**: Frame-by-frame video smoothness validator. Extracts frames, computes pixel diff, flags teleports + smoothness spikes. Threshold 12, rolling average check.
- **`e2e/audio-debug.spec.ts` / `audio-debug2.spec.ts`**: Diagnostic tests that proved AnalyserNode was NULL. 10 lines, solved what 8 iterations couldn't.
- **`e2e/sphere-test-ab.spec.ts`**: A/B comparison for GainNode vs synthetic signal sphere reaction.
- **`e2e/theme-topdown.spec.ts`**: 16 top-down theme + 8 sphere style screenshots.
- **`.claude/skills/marketing/demo-locked-spec.json`**: Single source of truth for all recording parameters.
- **`.claude/skills/marketing/shot-list-format.md`**: Standardized recording script format.

## Agent Infrastructure

- **4 specialized agents** created in `.claude/agents/`: 3d-visual, terminal-core, audio-voice, qa-ux
- Each with scoped tools, file ownership, `memory: project`
- **Locked spec pattern**: Mark agents read JSON spec, not paraphrased instructions

## Research & Analysis

- **Anthropic integration analysis**: 14 features evaluated, top 5 priorities ranked. HTML report (concise + verbose versions).
- **Performance analysis**: PbrHoloScene.tsx 3369-line god component, 35 useFrame hooks. 3 critical + 4 high issues. 7 quick wins identified. Tauri evaluated (7/10 vs Electron 8/10).
- **Memory separation architecture**: Agent-scoped cascade (9/10). Hal gets `memory: user`, HAL-O dev stays in project scope.
- **Recording solution research**: OBS WebSocket wins. obs-websocket-js installed.
- **Theme parameter analysis**: 6 styles × 12 params. 4 best slider candidates: bloom, vignette, grid, particles.

## Process Improvements

- **Frustration hook**: UserPromptSubmit regex on fuck/pissed/keeps failing → injects "STOP, analyze pattern, different approach"
- **SessionStart hook**: Auto-reads MEMORY.md on new session after compaction
- **1hr idle loop**: CronCreate job works backlog autonomously when user sleeps
- **Two-strike rule**: Same issue fails twice → diagnostic test mandatory before third attempt
- **Voice-in voice-out**: HARD RULE reinstated — TTS must generate before any text reply on Telegram
- **Progressive disclosure**: HTML reports show top pick visible, rest collapsed
- **Token routing**: Haiku for research/HTML, Opus for code

## Voice System

- Decision: **Hal** (butler) + **Hallie** (soft) — two voices only
- No gender labels anywhere (code, files, settings UI)
- V9 auto-rotation system to be removed
- Combined voice lines generated (no gap between greeting + open source)

## Rules Saved (10+)

- Voice priority on TG, context tags on every reply
- HTML on TG (never .md), embed images as base64
- Token saving (Haiku for research), subagent model routing
- User projects first ("sphere is vibe, cards are value")
- Honest assessment before technical fix
- Systemic fixes over patches
- Never wait when next step is obvious
- Mark reads locked spec JSON

## Screenshots

- 16 top-down theme comparisons (6 styles × colors)
- 16 front-view theme comparisons
- 8 sphere style close-ups
- V8 sphere reaction proof: silent vs speaking frame comparison

## Known Issues (Unfixed)

- Audio capture: OBS WebSocket setup needed (websocket not enabled in OBS settings)
- VB-Cable installed but ineffective for Electron audio routing
- Windows Terminal GPU rendering blocks GDI screenshot capture
- ~~Sphere styles 5/6/8 (corona, particles, lightning) not rendering~~ FIXED
- Hal-eye sphere doesn't scale well with audio (animated-core preferred)
- ~~0 commits this session — all work uncommitted~~ 4 commits pushed

## Session 7 — Post-compaction autonomous work (2026-03-26 03:00→)

**~14 hours total** (03:00 → 17:00 CET). Mix of autonomous + interactive. 39 commits pushed.

### Commits (23 total, all pushed)
1. `2ddd2d9` — 3 sphere styles (corona, particles, lightning) + 3 perf fixes + HOOK-LETMEKNOW
2. `064a188` — CSS keyframes extract + demo button toasts
3. `ad11687` — Demo terminal pre-fill (40% of feed on mount)
4. `cea9555` — Recap keyword hook + smart HTML delivery for TG/mobile
5. `efd9b9b` — Sphere styles test + devlog update
6. `0de9ee0` — Greeting templates (WELCOME1 prep)
7. `14b1969` — Voice cleanup — Hal + Hallie only (3 profiles, no gender labels)
8. `0141e8a` — Theme sliders — bloom, grid, particles, vignette user overrides (6 files)
9. `28784d4` — Devlog session 7 summary
10. `cee88b1` — WELCOME1 sphere greeting pulse on intro complete
11. `ad5b85f` — CSS card fly-in animation (classic renderer, 60ms stagger)
12. `f292c5e` — UX15 double-click card to resume session
13. `7d8a7fe` — Demo V9 OBS recording spec (ready for when OBS is configured)
14. README updated for voice cleanup (Hal + Hallie)
15. Devlog final update
16. CLAUDE.md — project rules for new sessions
17. B41 fix — dual-ring card overlap (inner radius capped, Y +0.8→+2.2)
18. CSS-INTRO fix — `i` → `globalIndex` (runtime crash in classic renderer)
19. Devlog 6hr tick
20. PERF: healthScrollY keyframe + activity glow → CSS class
21. PERF6: useHalEyeTexture rAF merged into R3F useFrame
22. SETTINGS-DOCK: dockable 360px right panel prototype
23. Gitignore: experimental test specs + debug logs

### Done
- **SPHERE-STYLES-FIX**: Corona Flare (torus rings), Particle Eruption (150-point fountain), Lightning Arcs (crackling bolts). All audio-reactive.
- **PERF #1**: BasicScreenPanel Vector3 scratch reuse (eliminates per-frame alloc)
- **PERF #4**: PbrRingPlatform uniforms useMemo
- **PERF #5**: SettingsMenu React.memo
- **PERF #2**: CSS keyframes extracted from ScreenPanel inline to App.css
- **DEMO-BUTTONS**: Toast "Demo project" on click instead of silent fail
- **DEMO-TERM**: Pre-fill terminals with 40% of feed on mount
- **HOOK-LETMEKNOW**: Opinion-seeking regex hook installed
- **HOOK-RECAP**: "recap" trigger word for conversation reformulation
- **VOICE-CLEANUP**: 21→3 profiles (AUTO, HAL, HALLIE). No gender labels.
- **THEME-SLIDERS**: 4 override sliders (bloom, grid, particles, vignette) with DEF/reset
- **WELCOME1 prep**: Greeting templates with time-of-day + absence-aware text
- **Sphere styles test**: e2e/sphere-styles.spec.ts validates all 8 styles
- **Smart HTML delivery**: html keyword checks afk_sticky → TG attachment when mobile
- **CSS-INTRO**: Staggered card fly-in (0.5s ease-out, 60ms per card)
- **UX15**: Double-click card → resume session
- **Demo V9 OBS spec**: Recording spec ready for OBS WebSocket
- **README**: Updated for voice cleanup (Hal + Hallie references)
- **B42**: Verified already correct (absorb button hidden when no external session)
- **Shipping priorities HTML**: Interactive tier-rated page with honest assessment
- 4 feedback memories saved (instant ack, shorter voice acks, TG file delivery, TG mode delivery)
- **CLAUDE.md**: Project rules file created (stack, hooks order, perf rules, voice, demo, CI)
- **B41**: Dual-ring card overlap fix (inner ring capped outerRadius-3, Y +2.2)
- **CSS-INTRO bugfix**: `i` not defined → `globalIndex` (runtime crash in classic renderer)
- **PERF6**: useHalEyeTexture separate rAF loop merged into R3F useFrame (unified timing)
- **SETTINGS-DOCK**: Dockable 360px right panel. Toggle in header. Scene visible while tweaking. Persisted.
- **Gitignore**: 12 experimental test specs + debug logs excluded from repo
- **Backlog**: 16+ items marked done, all S-tier shipping items complete

### Issues Found & Fixed
1. **CSS-INTRO `i` undefined**: cardFlyIn animation used `i` (from map callback) inside `renderCard` which takes `globalIndex`. TSC didn't catch it (template literal, runtime error). Stale vite cache masked the bug during initial smoke tests — only caught after clean rebuild. **Lesson**: always clear vite cache before smoke tests.
2. **CI auto-cancel storm**: 18 rapid pushes to master caused all CI runs to cancel each other (concurrency group behavior). No actual test results until manual re-run. Latest re-run: Linux 19/19 PASS, Windows 11/17 PASS (cancelled during artifact upload, 0 test failures).

### Process Improvements Applied
- Two-strike rule confirmed valuable: smoke test caught CSS-INTRO bug
- Vite cache added to mental checklist: clear before every smoke test
- Feedback memories: instant TG ack, shorter voice acks, TG file delivery, smart html keyword
