# Session Summary — 2026-03-24

## Session 2 — Massive feature + optimization wave (40+ tasks)

### Strategic (S2-S4)
- Versioned rules with "update available" badges in hub + 3D panels
- Community setup detection (Cursor, aider, Copilot, Windsurf)
- README rewrite — "works with any project" positioning

### Wizard / Onboarding (U2, U3, W5, X5)
- Modular feature picker in configure screen (8 sections, badges)
- Auto-detect stack from project files (package.json, pyproject, Cargo, go.mod, etc.)
- Quick create wizard path (name + stack → go)
- Compaction tip + PreCompact hook + CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=80

### UX Improvements (U5, U6, U8-U10, U12-U16)
- Settings reorganized into 6 collapsible sections
- EffectComposer key-based remount fix (root cause, not just error boundary)
- Wizard font size CSS pass (29 classes scale with --wizard-font-size)
- Zero-token-cost badges on wizard steps
- Favorite projects with ★ indicator + sorting
- Particle density slider: 11 levels (NONE→MAX)
- Ship VFX on/off toggle
- FONTS section recommendation (not yet implemented)
- Render quality slider (DPR 0.5x→NATIVE) — fixes 4K pixelation
- Save current view / Reset view buttons (replaced camera sliders)

### 3D Visual (A9, A12, procedural ring)
- Star Destroyer capital ship — 28 parts, ExtrudeGeometry hull, bridge tower, 3 engines
- Ship mesh native orientation fix — removed 180° hack
- Procedural ring shader — infinite resolution, zero texture file, theme-reactive
- Scene loading sequence — CSS overlay + 4-phase fade-in (structure → content → atmosphere → PostFX)

### Performance (PERF1-3, baseline infra)
- PERF1: Back-face culling — hide edge glow/HTML for back-facing panels (-72% draw calls at 100 cards)
- PERF2: Shared geometry — 4 shared geometries replace 600 per-panel instances (-87% geos)
- PERF3: IPC stampede cap — max 3 concurrent getProjectStats calls
- r3f-perf integration (F2 toggle), PerfStatsExporter (renderer.info + CPU + memory)
- Perf smoke + full test suites, automated baseline reports
- Camera orbit hitch fix — DOM writes only on state change (900/s → ~10/s)
- Demo card expansion to 100 (duplicate demo projects)

### Voice (V4, V4-wire)
- Sphere audio-reactive animation (FFT → scale/glow/rotation/pulse)
- TTS→analyser bridge for all audio sources (terminal, settings preview, voice controller)
- Demo mode: window.__haloAudioDemo = true

### Infrastructure (B2, B3, P7-P10, P12-P13)
- Docker: 6 fixes (missing PNGs, sandbox, Playwright, libasound, volumes)
- CI: Playwright install, split test steps, perf tests excluded, 60s timeout
- Hours methodology with multiplier table by task type
- Profiling rule file (Three.js, web, Python tools + baseline protocol)
- Perf dashboard HTML (Chart.js, 4 sections, filters)
- Channel-mode + voice-mode enforcement hooks in generated settings
- Sticky afk pattern, "no human effort bias" in generated CLAUDE.md
- Responsive topbar (3 CSS breakpoints)

### Bugs Fixed
- B11: Floor clipping — panel Y clamped to 0.5
- B13: Perf overlay moved to bottom-left
- B15: Responsive topbar
- Camera shake removed from flyby
- Ship nose-forward fix
- Test fixes: 10 stale selectors, setup-fresh guards, settings section expand helpers
- SettingsMenu syntax error (missing }})
- Card count slider max 30→100

### Rules & Memory
- Channel mode hook (UserPromptSubmit + PreToolUse) with sticky afk
- Voice mode hook (detect voice → inject additionalContext)
- "Best in class, beat it, ship it" — HARD RULE
- "No human effort bias" — HARD RULE
- "Always test even small changes" — HARD RULE
- "Always think at scale" — HARD RULE
- "Visual verification before presenting" — HARD RULE

### Audit
- Full 10-area competitive audit (UX, visual, perf, terminal, voice, settings, project mgmt, code quality, dev exp, features)
- Interactive HTML audit report with radar chart, comment boxes, priority selectors
