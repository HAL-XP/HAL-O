# HAL-O Devlog — 2026-03-27 (Session 8)

**~2 human hours** (22:00 → 00:30 CET). Post-compaction from session 7. Module architecture + multi-model implementation sprint.

---

## Work Done

### Module Architecture Brainstorm
- 7 modules defined: DEV, PLANNER, PERSONAL, OPS, CREATIVE, RESEARCH, COMMS
- Key insight: modules = task TYPE, not work vs personal. "Work" and "Personal" are cosmetic tags.
- Interactive HTML with expandable cards, user profiles, cost breakdown, dispatch flow diagram
- Architecture diagram HTML (SVG class-diagram): dispatcher → models → modules → memory
- Two side-by-side config examples: Full Local ($0) vs Hybrid ($50)
- Concept doc saved: `_concepts/module-architecture.md`

### Ollama + Local Models
- Ollama 0.18.3 installed via winget
- Qwen3:1.7B pulled and tested (9ms response with `think:false`)
- `model-providers.ts` extended: `listOllamaModels()`, `pullOllamaModel()`, `ollamaChat()`
- `ipc-models.ts`: IPC for listing Ollama models, routing config save/load, pull, test chat
- Model routing config persisted to `~/.hal-o/model-routing.json`

### Settings > Models Tab
- New tab in SettingsMenu with CPU icon
- Provider status indicators (green/red dots)
- 4 preset buttons (Full Local / Budget / Hybrid / Claude Only)
- Per-role dropdowns (Dispatcher, Coder, Assistant, QA, Voice Rewrite)
- Ollama model list + pull buttons + test button
- Searchable (tab integrated with existing search system)

### Dispatcher Engine
- `src/main/dispatcher.ts` — 5-layer cascade (Layers 0-2 implemented):
  - Layer 0: @prefix routing (`@hal-o fix this`)
  - Layer 1: keyword+project (`push client-api`), voice switch (`work on react app`), natural name detection (`fix auth in my react app`)
  - Layer 2: context stickiness (5min window)
- `src/main/ipc-dispatcher.ts` — IPC bridge for renderer
- Hyphen/space normalization: "my-react-app" matches "my react app" in speech
- 33 tests: prefix, keyword, voice switch, natural detection, ambiguous examples
- Smart focus logic: focused terminal wins, only @prefix overrides

### Own Telegram Bot Handler
- `src/main/telegram-handler.ts` (~160 lines)
- Own polling loop in Electron main process (replaces Claude's `--channels`)
- Flow: receive TG message → transcribe voice → dispatch → forward to right terminal → react 👍
- Voice switch commands via TG ("switch to client api", "list projects")
- `--channels` flag removed from non-HAL terminals

### Voice Dispatch Wiring
- HudTopbar `handleTranscript` wired through dispatcher
- Visual feedback toast: "Voice → project-name (Layer X, 85%)"
- Mic no longer requires HAL session — works with any terminal
- HUD label: "DISPATCH" instead of "HAL/NO LINK"
- Safe routing: focused terminal = default, only explicit @prefix overrides

### UX Critic Agent (Background)
- Spawned Sonnet agent to audit Settings + propose 3D visualization redesign
- 89KB HTML delivered: current settings rated 4.9/10, 3 redesign options with SVG wireframes, 3 hub visualization concepts (satellite spheres 8.5/10, stacked cards 7/10, color clusters 5.5/10)

## Files Created/Modified

### New Files
- `src/main/dispatcher.ts` — Message routing engine
- `src/main/ipc-dispatcher.ts` — Dispatcher IPC handlers
- `src/main/telegram-handler.ts` — Own TG bot handler
- `e2e/dispatcher.spec.ts` — 33 dispatcher tests
- `_concepts/module-architecture.md` — Module plan
- `temp/modules-brainstorm.html` — Interactive module brainstorm
- `temp/module-architecture-diagram.html` — SVG architecture diagram
- `temp/roadmap-memory-isolation.html` — Phase roadmap
- `temp/ux-models-proposal.html` — UX critic proposal

### Modified Files
- `src/main/model-providers.ts` — Ollama list/pull/chat + think:false
- `src/main/ipc-models.ts` — Routing config save/load, presets, Ollama IPCs
- `src/main/ipc-handlers.ts` — Register dispatcher handlers
- `src/main/index.ts` — Start/stop TG handler on app lifecycle
- `src/preload/index.ts` — Dispatcher + model IPC bridges
- `src/renderer/src/types.ts` — ModelRoutingConfig, dispatcher types
- `src/renderer/src/components/SettingsMenu.tsx` — Models tab
- `src/renderer/src/components/HudTopbar.tsx` — Dispatch-aware voice routing
- `src/renderer/src/hooks/useTerminalSessions.ts` — TG channel flag only on HAL

## Issues Found
- Qwen3 uses thinking mode by default → empty responses. Fixed: `think: false` in API call.
- Voice focus hard-linked to HAL session → mic disabled without HAL terminal. Fixed: mic always enabled.
- Voice dispatch to wrong terminal when project name appears in speech. Fixed: focused terminal wins, only explicit @prefix overrides.
- `--channels` on all terminals caused TG messages to go to wrong project. Fixed: restricted to HAL only + own TG handler.

## Commits
- 0 so far (uncommitted — will commit after user tests)

## Next (Session 8)
- User testing dispatch with real projects
- Update GitHub repo description (new acronym: "Holographic AI Launchpad")
- Commit all session 8 work
- Layer 4 (Qwen3 LLM for ambiguous messages) — when needed
- UX redesign implementation based on critic proposal

---

# Session 9 — 2026-03-27 (continued)

**~18 human-away hours** (00:30 → 18:50 CET). Autonomous mode for most of the session.

## Commits: 6 (13,000+ lines added)

| Hash | Summary |
|------|---------|
| fdb3932 | Halo Chat PWA + HTTP API + agent framework |
| 1b50cce | Session bridge — same brain everywhere |
| 7060389 | HaloNode recursive tree data model + CRUD |
| 510cb83 | ManageProjects React page + tree preload IPC |
| 0b0dba3 | Wire ManageProjects into app navigation |

## Major Features

### Halo Chat PWA (30+ features)
Mobile messaging app with holographic glassmorphism UI. Per-agent chat threads, Anthropic API backend, streaming responses, sentence-level TTS, voice recording, image upload (Vision API), reactions, swipe-to-reply, edit/regenerate, search, model selection, Service Worker, PWA manifest, light/dark theme.

### HTTP Control API (15+ endpoints)
REST + WebSocket on localhost:19400. HTTPS proxy on 19401 (self-signed cert for mic). Voice, chat, tree, terminal, dispatch endpoints.

### Tree Architecture
HaloNode recursive model. Persistent storage. Full CRUD via IPC + REST. Auto-migration from aliases.json. ManageProjects React page with top-down card layout, SVG connections, detail panel, import modal.

### Session Bridge
/chat/session routes through HAL terminal. response-capture.ts. Bob/Karen as personas. Fallback with warning.

### Research
Security audit (13 findings). Competitive analysis (30+ products). 5 interactive HTML reports. Architecture design documents.

## Key Decisions
- Bob/Karen = personas now, dispatchers later
- Recursive tree from day 1 (no level cap)
- Card/node layout for Manage Projects (not just tree list)
- Same session everywhere (terminal → PWA → Telegram)

## Bugs Fixed
- Dispatcher default fallback, HoloScene context menu, TG token leak, TTS wrong-message, GPU contention, PWA viewport
